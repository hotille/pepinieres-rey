{% extends 'base.html.twig' %}

{% block title %}Glossaire{% endblock %}

{% block body %}
    <h1>Glossaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ glossaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ glossaire.nom }}</td>
            </tr>
            <tr>
                <th>Nom_latin</th>
                <td>{{ glossaire.nomLatin }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ glossaire.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ glossaire.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('glossaire_index') }}">back to list</a>

    <a href="{{ path('glossaire_edit', {'id': glossaire.id}) }}">edit</a>

    {{ include('glossaire/_delete_form.html.twig') }}
{% endblock %}
